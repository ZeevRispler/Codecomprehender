# Remove these unused dependencies:
pyyaml>=6.0  # Not using YAML config anymore
aiohttp>=3.8.0  # Not using aiohttp
asyncio-throttle>=1.0.0  # Not implementing throttling
pytest>=7.0.0  # Development dependencies
black>=22.0.0
flake8>=4.0.0
rich>=12.0.0  # Not using rich console output